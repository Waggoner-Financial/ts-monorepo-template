// Placeholder export so the template's build/test/typecheck pipeline has
// something real to exercise. Replace with actual library code.
export function greet(name: string): string {
  return `Hello, ${name}!`;
}
